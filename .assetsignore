# Files in the repo/output dir that must NOT be served as static assets.
# (worker.js + functions/ are bundled into the Worker; the rest is tooling/docs.)
node_modules
.git
.wrangler
*.log
worker.js
functions
migrations
package.json
wrangler.toml
wrangler.toml.template
.dev.vars
.dev.vars.example
.gitignore
.assetsignore
CLAUDE.md
SETUP.md
